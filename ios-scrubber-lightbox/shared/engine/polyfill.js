// Marked on the document by scroll-timeline-loader.js, which runs before any
// module does, when it loads the scroll-timeline polyfill.
export const usingScrollTimelinePolyfill = document.documentElement.hasAttribute("data-scroll-timeline-polyfill");
