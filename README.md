# Carousels and Lightboxes

Blank React app skeleton (Vite + React), cleared for a fresh start.

## Structure

```
ios-scrubber-lightbox/   the app
```

## Running locally

```bash
cd ios-scrubber-lightbox
npm install
npm run dev
```
